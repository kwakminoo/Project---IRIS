[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: Windows GUI 통합 테스트 (기본 실행 제외)
    windows_only: Windows에서만 실행되는 테스트
    windows_smoke: 실제 Windows 앱·창·입력 스모크 테스트 (기본 실행 제외)
    slow: 상대적으로 오래 걸리는 테스트
filterwarnings =
    ignore::DeprecationWarning
